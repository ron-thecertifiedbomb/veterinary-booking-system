services:
  web:
    image: nginx:1.27-alpine
    container_name: ${VET_WEB_CONTAINER:-rondev-vet-web-prod}
    ports:
      - "${VET_WEB_PORT:-8093}:80"
    volumes:
      - ../../dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
